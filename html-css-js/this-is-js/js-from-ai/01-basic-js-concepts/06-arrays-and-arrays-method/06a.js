// Create an array of your favorite fruits and print them to the console.
let myFruit = ["Apple", "Orange", "Watermelon"];
console.log(myFruit);
for (let i = 0; i < myFruit.length; i++) {
    console.log(`0${i+1}. ${myFruit[i]}`);
}