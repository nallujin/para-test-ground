// Create a variable "isAdult" and assign it a boolean value that indicates whether you are an adult. Use comparison operators to determine this.
let isAdult = true;
if (isAdult) {
    console.log(`You're an adult`);
} else {
    console.log(`You're NOT an adult`);
}